{
  "name": "@ai-builder/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@ai-builder/schemas": "workspace:*",
    "@ai-builder/serializers": "workspace:*",
    "@ai-builder/types": "workspace:*",
    "@ai-sdk/openai": "^3.0.71",
    "@cloudflare/agents": "^0.0.16",
    "@upstash/vector": "^1.1.5",
    "ai": "^6.0.205",
    "braintrust": "^0.0.160",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260613.1",
    "typescript": "^5.6.3",
    "wrangler": "^4.100.0"
  }
}
