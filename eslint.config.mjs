import js from "@eslint/js";
import ts from "typescript-eslint";
import reactHooks from "eslint-plugin-react-hooks";
import reactRefresh from "eslint-plugin-react-refresh";

export default ts.config(
  {
    ignores: [
      "**/dist/**",
      "**/.wrangler/**",
      "**/node_modules/**",
      "**/.turbo/**",
      // evals/ is a standalone Node.js project with its own tsconfig and type requirements
      "evals/**",
      // Config files at root — not part of any tsconfig project
      "*.mjs",
      "*.cjs",
      "apps/web/postcss.config.js",
      // Auto-generated by @tanstack/router-plugin — must not be linted
      "**/routeTree.gen.ts",
      // Node.js CLI script excluded from worker tsconfig — not part of any project service
      "apps/worker/src/rag/index-corpus.ts",
    ],
  },
  js.configs.recommended,
  ...ts.configs.strictTypeChecked,
  ...ts.configs.stylisticTypeChecked,
  {
    plugins: {
      "react-hooks": reactHooks,
      "react-refresh": reactRefresh,
    },
    rules: {
      ...reactHooks.configs.recommended.rules,
      "react-refresh/only-export-components": ["warn", { allowConstantExport: true }],
      "@typescript-eslint/no-explicit-any": "error",
      "@typescript-eslint/no-floating-promises": "error",
      "@typescript-eslint/no-unused-vars": ["error", { argsIgnorePattern: "^_" }],
      "@typescript-eslint/consistent-type-imports": ["error", { prefer: "type-imports" }],
      "@typescript-eslint/no-import-type-side-effects": "error",
    },
    languageOptions: {
      parserOptions: {
        projectService: true,
        tsconfigRootDir: import.meta.dirname,
      },
    },
  },
  // Non-React packages: disable react-specific rules
  {
    files: ["packages/**/*.ts", "apps/worker/**/*.ts", "evals/**/*.ts"],
    rules: {
      "react-hooks/rules-of-hooks": "off",
      "react-hooks/exhaustive-deps": "off",
      "react-refresh/only-export-components": "off",
    },
  },
  // Test files — vitest matchers (expect.any, expect.stringContaining) return `any` by design
  {
    files: ["**/*.test.ts", "**/*.test.tsx"],
    rules: {
      "@typescript-eslint/no-unsafe-assignment": "off",
    },
  },
  // shadcn/ui generated files — they intentionally export constants alongside components
  // and use ElementRef which is deprecated in React 19 (no auto-fix available)
  {
    files: ["apps/web/src/components/ui/**/*.tsx"],
    rules: {
      "react-refresh/only-export-components": "off",
      "@typescript-eslint/no-deprecated": "off",
    },
  }
);
